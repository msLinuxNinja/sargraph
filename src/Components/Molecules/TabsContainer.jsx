export default function TabsContainer({ children }) {

  return (
    <main className="w-full h-full my-auto top-0 content-center items-center justify-center p-3 overflow-y-auto">
      {children}
    </main>
  );
}